---
import BaseLayout from "../layouts/BaseLayout.astro";
import Navbar from "../components/Navbar.astro";
---

<BaseLayout title="Fabián | Portafolio">
  <Navbar />

  <main class="max-w-6xl mx-auto px-6">

    <!-- HERO -->
    <section class="min-h-[80vh] flex flex-col justify-center
                    text-center md:text-left">
      <span class="reveal opacity-0 translate-y-8 transition-all duration-700
                   text-indigo-500 text-sm uppercase tracking-widest mb-4">
        Desarrollador Web
      </span>

      <h1 class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-100
                 text-3xl sm:text-4xl md:text-6xl font-extrabold leading-tight mb-6">
        Construyo <span class="text-indigo-500">experiencias web</span><br />
        rápidas y escalables
      </h1>

      <p class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-200
                text-zinc-600 dark:text-zinc-400 text-base sm:text-lg
                max-w-xl mx-auto md:mx-0">
        Desarrollo aplicaciones modernas con Angular, NestJS, Astro y Firebase.
        Me enfoco en rendimiento, diseño limpio y buenas prácticas.
      </p>

      <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-300
                  mt-10 flex flex-col sm:flex-row gap-4
                  justify-center md:justify-start">
        <a
          href="/projects"
          class="px-6 py-3 bg-indigo-500 rounded-xl text-white font-medium
                 hover:bg-indigo-600 transition"
        >
          Ver proyectos
        </a>

        <a
          href="https://github.com/tuusuario"
          target="_blank"
          class="px-6 py-3 border border-zinc-300 dark:border-zinc-700
                 rounded-xl hover:bg-zinc-100 dark:hover:bg-zinc-800 transition"
        >
          GitHub
        </a>
      </div>
    </section>

    <!-- STACK -->
    <section class="py-20 border-t border-zinc-200 dark:border-zinc-800">
      <h2 class="reveal opacity-0 translate-y-8 transition-all duration-700
                 text-2xl font-bold mb-8">
        Stack principal
      </h2>

      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4 sm:gap-6">
        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-100
                    p-4 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:scale-105 transition-transform">
          Angular
        </div>
        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-200
                    p-4 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:scale-105 transition-transform">
          NestJS
        </div>
        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-300
                    p-4 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:scale-105 transition-transform">
          Astro
        </div>
        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-400
                    p-4 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:scale-105 transition-transform">
          Firebase
        </div>
      </div>
    </section>

    <!-- PROJECTS -->
    <section class="py-20 border-t border-zinc-200 dark:border-zinc-800">
      <h2 class="reveal opacity-0 translate-y-8 transition-all duration-700
                 text-2xl font-bold mb-8">
        Proyectos destacados
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-100
                    p-6 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:border-indigo-500 hover:-translate-y-1 hover:shadow-lg transition">
          <h3 class="font-semibold text-lg mb-2">Marketplace educativo</h3>
          <p class="text-zinc-600 dark:text-zinc-400 text-sm mb-4">
            Plataforma para docentes con marketplace, verificación y pagos.
          </p>
          <span class="text-xs text-indigo-500">Angular · NestJS · MySQL</span>
        </div>

        <div class="reveal opacity-0 translate-y-8 transition-all duration-700 delay-200
                    p-6 border border-zinc-200 dark:border-zinc-800 rounded-xl
                    hover:border-indigo-500 hover:-translate-y-1 hover:shadow-lg transition">
          <h3 class="font-semibold text-lg mb-2">App de asistencia QR</h3>
          <p class="text-zinc-600 dark:text-zinc-400 text-sm mb-4">
            Registro de asistencia con QR y Google Sheets.
          </p>
          <span class="text-xs text-indigo-500">Android · Firebase</span>
        </div>
      </div>
    </section>
  </main>

  <!-- Scroll reveal -->
  <script>
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.remove("opacity-0", "translate-y-8");
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.15 }
    );

    document.querySelectorAll(".reveal").forEach((el) => observer.observe(el));
  </script>
</BaseLayout>
